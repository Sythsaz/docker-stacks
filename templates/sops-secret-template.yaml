# SOPS + age Secret Template
# Usage:
#   1. Copy this file to stacks/<stack>/secrets/<name>.yaml
#   2. Fill in values
#   3. Encrypt: sops --encrypt --in-place stacks/<stack>/secrets/<name>.yaml
#   4. The encrypted file (.enc) is safe to commit to Git
#
# To decrypt for deployment:
#   sops --decrypt stacks/<stack>/secrets/<name>.enc > stacks/<stack>/secrets/<name>.txt

# Example secret values (REPLACE before encrypting)
db_password: "CHANGE_ME_BEFORE_ENCRYPTING"
api_key: "CHANGE_ME_BEFORE_ENCRYPTING"
